# Secrets — never commit. This repo is public.
.env
.env.*
!.env.example

# Python
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/

# macOS
.DS_Store

# Run artifacts — regenerable. Curated trajectories for submission are
# committed explicitly under runs/curated/ (see steps.md 7.3).
runs/*
!runs/curated/
!runs/.gitkeep
